{
  "name": "setBatchActive",
  "description": "Activate or deactivate a batch that belongs to a study.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "studyIdOrUuid": {
        "type": "string",
        "description": "Either a numeric study ID or a study UUID"
      },
      "batchId": {
        "type": "integer",
        "description": "Numeric batch ID"
      },
      "active": {
        "type": "boolean",
        "description": "Whether the batch should be active or not"
      }
    },
    "additionalProperties": false,
    "required": [
      "active",
      "batchId",
      "studyIdOrUuid"
    ]
  },
  "outputSchema": {
    "type": "object",
    "properties": {
      "data": {
        "type": "object",
        "properties": {
          "success": {
            "type": "boolean",
            "description": "True if the operation completed successfully."
          },
          "message": {
            "type": "string",
            "description": "A message that describes the result of the operation."
          }
        },
        "required": [
          "success"
        ],
        "additionalProperties": false
      },
      "warnings": {
        "type": "array",
        "items": {
          "type": "string"
        }
      }
    },
    "required": [
      "data"
    ],
    "additionalProperties": false
  },
  "metadata": {
    "requiredUserRole": "USER",
    "openapi": {
      "operationId": "setBatchActive"
    }
  }
}
