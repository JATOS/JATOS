{
  "name": "deleteStudyAssetsFile",
  "description": "Delete a file from the study assets",
  "inputSchema": {
    "type": "object",
    "properties": {
      "studyIdOrUuid": {
        "type": "string",
        "description": "Either a numeric study ID or a study UUID"
      },
      "filepath": {
        "type": "string",
        "description": "Path to the file in the study assets directory that is supposed to be deleted. Directories won't be deleted."
      }
    },
    "additionalProperties": false,
    "required": [
      "filepath",
      "studyIdOrUuid"
    ]
  },
  "outputSchema": {
    "type": "object",
    "properties": {
      "data": {
        "type": "object",
        "properties": {
          "success": {
            "type": "boolean",
            "description": "True if the operation completed successfully."
          },
          "message": {
            "type": "string",
            "description": "A message that describes the result of the operation."
          }
        },
        "required": ["success"],
        "additionalProperties": false
      },
      "warnings": {
        "type": "array",
        "items": {
          "type": "string"
        }
      }
    },
    "required": [
      "data"
    ],
    "additionalProperties": false
  },
  "metadata": {
    "requiredUserRole": "USER",
    "openapi": {
      "operationId": "deleteStudyAssetsFile"
    }
  }
}
